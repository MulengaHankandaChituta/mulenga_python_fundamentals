"""Write a program in a file named triangle.py to compute. Issue the appropriate prompts
for the triangle's base and height. Then use the formula .5 * base * height to compute the area
Test the program from an IDLE window."""

base = float(input("Enter the base: "))
height = float(input("Enter the height: "))
area = 0.5 * base * height
print(area)